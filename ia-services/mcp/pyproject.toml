[project]
name = "mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.9.0",
    "requests>=2.32.3",
    "mcp>=1.1.3",
]

[project.scripts]
mcp-server = "src.server:main"
